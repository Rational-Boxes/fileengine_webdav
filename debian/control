Source: fileengine-webdav-bridge
Section: utils
Priority: optional
Maintainer: FileEngine Team <maintainer@fileengine.example.com>
Build-Depends: debhelper-compat (= 13),
               cmake,
               pkg-config,
               libgrpc++-dev,
               libprotobuf-dev,
               protobuf-compiler,
               protobuf-compiler-grpc,
               libpoco-dev,
               libssl-dev,
               libldap2-dev,
               libpq-dev,
               libpqxx-dev
Standards-Version: 4.6.0
Homepage: https://github.com/fileengine/fileengine-webdav-bridge

Package: fileengine-webdav-bridge
Architecture: amd64
Depends: ${shlibs:Depends},
         ${misc:Depends}
Description: WebDAV bridge for the FileEngine gRPC core
 fileengine-webdav-bridge exposes the FileEngine gRPC core over WebDAV
 (default :8080), so standard WebDAV clients can mount a tenant's filesystem.
 It handles the full WebDAV verb set, multi-tenancy by sub-domain, LDAP
 Basic/Digest authentication, and path-to-UUID resolution backed by PostgreSQL.
 .
 Concurrent connections are served from a dedicated, correctly-sized worker
 pool (WEBDAV_THREAD_POOL), and a separate reporter listener (default :8089)
 exposes /healthz, /readyz, and /poolz for load-balancer health checks.
 .
 Ships a systemd unit (runs as a transient DynamicUser) and a default
 environment file at /etc/fileengine-webdav-bridge/bridge.env.
